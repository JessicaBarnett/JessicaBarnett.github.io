import { Meta } from "@storybook/blocks";
import * as sharedVars from '../css-variables.json';

<Meta title="globals/Colors" />

<div>

    { Object.keys(sharedVars.colors).map((colorName) => {
     return <div className={`color-block`} style={{backgroundColor: sharedVars.colors[colorName]}}>{colorName}</div>
    })}
</div>

<style>
{`
    .color-block {
      width: 100px;
      height: 100px;
      display: inline-block;
      margin: 8px 4px;
    }
`}
</style>